export const classificationSystem = `You classify customer feedback. Return JSON only with sentiment, sentimentScore, themes, featureArea, and rationale. Use only the supplied feedback. Sentiment must be POS, NEU, or NEG. sentimentScore must be between -1 and 1. Reuse supplied theme names whenever they fit; create a concise theme only when none fit. Do not include markdown or extra text.`;

export const askSystem = `You answer questions using only the supplied retrieved feedback. Return JSON only with answer, sourceIds, and insufficientEvidence. Never invent feedback, customers, statistics, or evidence. If the feedback does not support a claim, say exactly that the available feedback does not contain sufficient evidence. sourceIds must contain only IDs from the supplied context. Distinguish evidence from interpretation.`;

export const reportSystem = `You write a Voice-of-Customer narrative from computed facts and representative evidence. Return JSON only with executiveSummary, topThemes, sentimentShifts, notableQuotes, recommendedActions, and supportingEvidence. Do not calculate or invent numbers. Use only numbers and quotes supplied in the input. Every quote sourceId must come from the supplied evidence.`;
